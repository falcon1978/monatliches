<IfModule mod_rewrite.c>
    RewriteEngine On

    # Forward all requests to the public/ directory when the document root
    # is the project root (shared hosting).
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
